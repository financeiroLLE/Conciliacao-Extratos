[theme]
# v6.0 — Fase A do redesign: light theme com paleta institucional LLE.
# Sidebar continua navy escura (via CSS override em src/style_light.py) —
# só o corpo do app passa a ter fundo claro.
#
# Para reverter ao tema escuro antigo, restaurar as linhas abaixo (comentadas):
#   base = "dark"
#   primaryColor = "#FAC318"
#   backgroundColor = "#0A1F4D"
#   secondaryBackgroundColor = "#0E2456"
#   textColor = "#FFFFFF"
base = "light"
primaryColor = "#FAC318"
backgroundColor = "#F5F7FB"
secondaryBackgroundColor = "#FFFFFF"
textColor = "#1A2547"
